name: SystèmeGPT Quality Check

on:
  pull_request:
    branches: [ main ]
  push:
    branches: [ dev ]

jobs:
  quality:
    runs-on: ubuntu-latest

    steps:
    - name: 🧩 Checkout Repository
      uses: actions/checkout@v3

    - name: 🧱 Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'

    - name: 📦 Install dependencies
      run: npm install

    - name: 🔍 Run lint
      run: npm run lint || echo "No lint configured"

    - name: 🧠 Build and verify system
      run: npm run build || echo "No build configured"

    - name: ✅ Quality check passed
      run: echo "SystèmeGPT check complete"